spring:
  mongodb:
    uri: mongodb://resustack:resustack@localhost:27018/resustack?authSource=admin

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.springframework.data.mongodb: DEBUG  # 전체 Spring Data MongoDB 로깅
